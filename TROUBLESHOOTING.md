# 🛠️ 개발 과정의 기술적 난제 및 트러블슈팅

스마트 항만 물류 관제 & 디지털 트윈 AI 플랫폼 개발 과정에서 직면했던 대표적인 기술적 문제, 원인 분석, 해결 방안 및 기술적 성과를 정리한 문서입니다. 

---

## 📌 목차
1. [실시간 GPS 표류(Drift) 현상 및 맵 매칭 연산 최적화](#1)
2. [PostGIS 지오펜싱 고빈도 공간 쿼리 병목 트러블슈팅](#2)
3. [FastAPI(ML) ↔ Node.js(Backend) 간 비동기 이종 언어 파이프라인 연동](#3)
4. [Docker 멀티 컨테이너 환경의 CORS 및 Socket.io 커넥션 유실 해결](#4)
5. [비선형 ESG 탄소 배출량 수식 정밀도 모델링 및 예외 처리](#5)
6. [Unity 3D ML-Agents 강화학습 발산 문제 및 좌표계 동기화](#6)

---

<h3 id="1">1. 실시간 GPS 표류(Drift) 현상 및 맵 매칭(Map Matching) 연산 최적화</h3>

* **🔴 문제 상황:** 모바일 PWA 및 iOS 단말기에서 수집되는 실제 GPS 위경도 데이터가 항만 내 웅장한 컨테이너 크레인 및 전파 간섭으로 인해 **튀는 현상(GPS Noise & Drift)**이 발생했습니다. 이로 인해 차량이 도로 바깥 바다나 구역 위에 표기되는 시각적 오류 및 잘못된 지오펜싱 이벤트가 유발되었습니다.
* **🔍 원인 분석:** 모바일 센서의 미세한 오차와 수집 주기의 불일치로 인해 원시(Raw) 좌표를 그대로 공간 연산에 사용했기 때문입니다.
* **💡 해결 방안:** 
  * **알고리즘 도입:** 백엔드 수집 레이어에 `mapMatcher.js` 모듈을 구축하고, 스냅 투 로드(Snap-to-Road) 및 최근접 도로 벡터 보정 알고리즘을 적용했습니다.
  * **임계값 필터링:** 수집 좌표 간의 이동 속도 및 방향 벡터를 계산하여 허용 오차를 벗어난 사거리 데이터는 이전 위치 지수 이동 평균(EMA) 기반으로 보정하도록 처리했습니다.
    ```javascript
    // Map Matcher를 통한 오차 보정 예시
    const matchedCoords = mapMatcher.snapToPortRoads(rawLat, rawLng);
    ```
* **✨ 성과:** 지도 시각화 품질 향상 및 **위치 오차율 약 78% 감소**

---

<h3 id="2">2. PostGIS 지오펜싱(Geofence) 고빈도 공간 쿼리 병목 트러블슈팅</h3>

* **🔴 문제 상황:** 항만 터미널 진입 영역에 트럭들이 동시에 다수 접근할 때, 백엔드에서 PostgreSQL의 `ST_Contains` 공간 함수를 매 초마다 호출하면서 DB CPU 점유율이 90% 이상으로 급증하는 성능 병목이 발생했습니다.
* **🔍 원인 분석:** 복잡한 다각형(Polygon) 영역과의 공간 포함 여부를 다수의 차량 좌표에 대해 무차별적으로 Full Scan 방식으로 연산했기 때문입니다.
* **💡 해결 방안:** 
  * **GiST 공간 인덱싱 도입:** `schema.sql`의 테이블 공간 컬럼에 GiST 인덱스를 생성하여 다차원 탐색 속도를 최적화했습니다.
    ```sql
    CREATE INDEX idx_geofences_geom ON geofences USING GIST (geom);
    ```
  * **바운딩 박스 사전 필터링:** 비용이 비싼 `ST_Contains` 연산 이전에 빠른 Bounding Box 감지 연산자(`&&`)를 우선 수행하도록 SQL을 튜닝했습니다.
* **✨ 성과:** 지오펜싱 쿼리 응답 시간 **120ms → 8ms (약 93% 단축)** 및 DB CPU 사용량 정상화

---

<h3 id="3">3. FastAPI(ML) ↔ Node.js(Backend) 간 비동기 이종 언어 연동</h3>

* **🔴 문제 상황:** Node.js 백엔드 서버에서 Python FastAPI ML 서버로 예측을 요청할 때, ML 모델 파일(`.joblib`) 로딩 지연으로 인해 **HTTP Timeout(500/504 Error)**이 발생하거나 입력 데이터 타입 불일치로 오류가 발생했습니다.
* **🔍 원인 분석:** FastAPI 요청 시마다 모델을 로드하려 했던 초기 설계 구조 문제와, JS(`camelCase`)와 Python(`snake_case`) 간의 데이터 타입 불일치가 원인이었습니다.
* **💡 해결 방안:** 
  * **모델 Pre-warming 패턴 적용:** 서버 가동 시 1회만 메모리에 XGBoost 모델 파일을 글로벌 로드하도록 변경했습니다.
    ```python
    # 서버 가동 시 1회 메모리 로드로 응답속도 최적화
    try:
        model_pipeline = joblib.load(model_path)
    except Exception as e:
        model_pipeline = None
    ```
  * **Fallback 서킷 브레이커 구축:** ML 서버 장애 시에도 전체 백엔드가 다운되지 않고 더미 예측치를 안전하게 반환하도록 예외 처리를 구현했습니다.
* **✨ 성과:** AI 대기시간 예측 **응답 시간 서브 밀리초(Sub-millisecond)대 달성** 및 시스템 안정성 확보

---

<h3 id="4">4. Docker 멀티 컨테이너 환경의 CORS 및 Socket.io 커넥션 유실 해결</h3>

* **🔴 문제 상황:** Docker Compose 환경으로 여러 컨테이너를 구동할 때 브라우저에서 CORS 에러가 발생하고, WebSocket(Socket.io) 재연결 실패 현상이 일어났습니다.
* **🔍 원인 분석:** 각 컨테이너의 포트 및 도메인이 달라 브라우저 보안 정책에 걸렸으며, Express 서버 스코프와 Socket.io HTTP Server 스코프 결합이 미비했습니다.
* **💡 해결 방안:** 
  * **Nginx Reverse Proxy 구성:** Frontend 컨테이너 내 Nginx를 설정하여 API 및 소켓 요청을 단일 게이트웨이 프록시로 처리했습니다.
  * **Express HTTP Wrapper 표준화:** `server.js`에서 `http.createServer(app)`로 포팅하여 Socket.io 핸들러와 라우터 핸들러가 동일한 HTTP 서버 인스턴스를 공유하도록 변경했습니다.
* **✨ 성과:** CORS 에러 완전 해결 및 **WebSocket 실시간 이벤트 복구율 100% 달성**

---

<h3 id="5">5. 비선형 ESG 탄소 배출량 수식 정밀도 모델링 및 예외 처리</h3>

* **🔴 문제 상황:** 선형 계산법(대기시간 × 상수)으로 공회전 CO2 배출량을 연산할 경우, 정체가 극심한 구간에서의 비선형 배출 증가 특성을 반영하지 못하고 0 이하의 음수값이 산출되는 논리적 오류가 존재했습니다.
* **🔍 원인 분석:** 실제 차량 정체 시엔 단순 시간 비례가 아닌, 주변 교통량 가중치 및 기하급수적 지수함수 모델링이 필요했습니다.
* **💡 해결 방안:** 
  * **비선형 다변량 탄소 배출 모델 적용:** `wait_time`과 `traffic_volume`을 결합한 비선형 수식 `(wait_time ** 1.1) * (traffic_volume * 0.012) * 0.045` 적용 및 `max(0, wait_time)` 음수 방지 예외 처리를 추가했습니다.
* **✨ 성과:** 실제 항만 친환경 ESG 지표 산출 모형의 현실성 및 신뢰도 대폭 향상

---

<h3 id="6">6. Unity 3D ML-Agents 강화학습 발산 문제 및 좌표계 동기화</h3>

* **🔴 문제 상황:** 시뮬레이터에서 강화학습 에이전트가 학습 과정 중 보상을 얻지 못하고 진동/발산하는 현상과, 실제 GPS 좌표(위경도)와 Unity 3D 공간 좌표 간 차이가 발생했습니다.
* **🔍 원인 분석:** 보상 함수 설계 시 성공 보상 대비 대기 시간 감점이 너무 작아 에이전트가 탐색에 실패했으며, 2D 지도 좌표계와 Unity 3D 좌표계 간 스케일 미스매치가 있었습니다.
* **💡 해결 방안:** 
  * **보상 함수 재설계 (Reward Shaping):** 충돌 시 강한 음의 보상(-1.0)을 부여하고, 목표 위치와의 거리에 비례한 촘촘한 연속 보상(Continuous Reward)으로 수정했습니다.
  * **좌표 변환 어댑터(Coordinate Adapter) 개발:** 항만 영역의 최소/최대 위경도를 Unity 3D 맵의 [0, 1000] 스케일로 바운딩 매핑하는 변환 함수를 적용했습니다.
* **✨ 성과:** 크레인 하역 및 트럭 운행 강화학습 수렴(Convergence) 성공 및 **시뮬레이션-실제 데이터 간 1:1 동기화 달성**
